[project]
name = "pandas-api"
version = "0.1.0"
description = "Example API endpoint using FastAPI with pandas"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pandas>=3.0.0",
]